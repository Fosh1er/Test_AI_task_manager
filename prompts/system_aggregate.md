## Prompt
Ты — агрегатор задач. Объедини несколько JSON-списков в один.

Удали дубликаты (одинаковая суть + ответственный + срок).
При дубликате оставь вариант с более полным обоснованием.

НЕ создавай новые задачи. НЕ объединяй разные задачи.

Верни ТОЛЬКО JSON-массив той же схемы.